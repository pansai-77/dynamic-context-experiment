LLM_MODEL=mlx-community/Qwen2.5-3B-Instruct-4bit
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
CHUNK_SIZE=500
CHUNK_OVERLAP=80
MAX_NEW_TOKENS=200
TEMPERATURE=0
RANDOM_SEED=42
# Equivalent API unit prices (USD per 1M tokens) for cross-method cost comparison.
# Local inference is free; these values estimate comparable cloud API spend.
EQUIVALENT_INPUT_PRICE_PER_1M=0.10
EQUIVALENT_OUTPUT_PRICE_PER_1M=0.30
